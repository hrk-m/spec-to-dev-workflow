# remove-group-members — メンバー一括削除

## 概要

グループ詳細画面のメンバー一覧で、各行のチェックボックスまたはヘッダーの全選択チェックボックスを使って 1 件または複数件のメンバーを選択し、「削除」ボタンで一括してグループから除名できる。削除後はメンバー数と一覧が自動的に更新される。

---

## 処理フロー（正常系）

```
ユーザーがチェックボックスで削除対象メンバーを選択する
  │
  ├─ [個別選択] メンバー行のチェックボックスをクリックして選択 / 解除を切り替える
  │
  └─ [全選択] ヘッダーチェックボックスをクリックする
       ├─ 全未選択の場合 → 全メンバーが選択される
       ├─ indeterminate（一部選択）の場合 → 全メンバーが選択される
       └─ 全選択の場合 → 全メンバーの選択が解除される
  │
  ├─ 「削除」ボタンが enabled になる（0 件の場合は disabled）
  ├─ ユーザーが「削除」ボタンをクリックする
  ├─ AlertDialog が表示される
  │    └─ 「選択した N 名をグループから削除しますか？」（N = 選択件数）
  ├─ ユーザーがダイアログの「削除する」をクリックする
  ├─ DELETE /api/v1/groups/:id/members { "user_ids": [...] } を送信する
  ├─ API が 204 No Content を返す
  ├─ チェック状態をリセットする（selectedIds をクリア）
  ├─ clearMemberListCache() でメンバー一覧キャッシュをクリアする
  ├─ GET /api/v1/groups/:id/members を再取得してメンバー一覧を更新する
  ├─ useGroupDetail.refetch() でグループ詳細（member_count）を更新する
  └─ 「削除」ボタンが再び disabled に戻る
```

---

## 処理フロー（キャンセル）

```
ユーザーがダイアログの「キャンセル」をクリックする
  │
  └─ AlertDialog が閉じる。チェック状態はそのまま維持される
```

---

## ヘッダーチェックボックスの状態派生

| 条件                             | ヘッダーチェックボックスの状態 |
| -------------------------------- | ------------------------------ |
| メンバー 0 件                    | disabled                       |
| メンバー 1 件以上 かつ 全未選択  | 未チェック                     |
| 0 < 選択数 < 全体数              | indeterminate（半選択）        |
| 選択数 = 全体数（かつ 1 件以上） | チェック済み                   |
| 削除成功後                       | 全解除（リセット）             |

---

## 処理フロー（異常系）

```
DELETE リクエストが 4xx・5xx を返す
  │
  └─ AlertDialog 内にエラーメッセージを表示する。ダイアログは開いたまま

対象グループが存在しない（404）
  │
  └─ AlertDialog 内に「グループが見つかりません」に相当するエラーを表示する

削除対象にグループメンバーでない user_id が含まれる（404）
  │
  └─ AlertDialog 内にエラーメッセージを表示する。削除は実行されない

メンバーが 0 件の場合
  │
  └─ ヘッダーチェックボックスが disabled になり、クリックできない

削除 API 失敗の場合
  │
  └─ 選択状態を維持したまま AlertDialog 内にエラーを表示する（ヘッダー状態は変化なし）
```

---

## 使用コンポーネント・状態

| 要素                   | 種別           | 役割                                                                                          |
| ---------------------- | -------------- | --------------------------------------------------------------------------------------------- |
| `MemberList`           | コンポーネント | 各行にチェックボックスを表示し、「削除」ボタンと削除確認 AlertDialog を内包する               |
| `GroupDetailContent`   | コンポーネント | `onRefetch` props 経由で `useGroupDetail.refetch()` を `MemberList` に渡す                    |
| `deleteGroupMembers`   | API 関数       | `DELETE /api/v1/groups/:id/members` を呼び出す                                                |
| `selectedIds`          | state          | チェックされたメンバー ID の集合（`Set<number>`）。「削除」ボタンの enabled / disabled を制御 |
| `isAllSelected`        | 派生 state     | 全メンバーが選択されているかを示すフラグ。ヘッダーチェックボックスの checked 状態に連動       |
| `isSomeSelected`       | 派生 state     | 一部のみ選択されているかを示すフラグ。ヘッダーチェックボックスの indeterminate 状態に連動     |
| `headerCheckboxRef`    | ref            | ヘッダー `<input type="checkbox">` への参照。`useEffect` で indeterminate を DOM に直接設定   |
| `handleSelectAll`      | イベント関数   | 全選択 / 全解除を切り替える。`isAllSelected` が true のとき全解除、それ以外は全選択           |
| `isDeleteDialogOpen`   | state          | 削除確認 AlertDialog の開閉状態                                                               |
| `deleteError`          | state          | 削除 API のエラーメッセージ                                                                   |
| `clearMemberListCache` | 関数           | メンバー一覧のクライアントキャッシュをクリアして再フェッチをトリガーする                      |

---

## 確認観点

```
- [ ] メンバー一覧のヘッダーに全選択チェックボックスが表示される
- [ ] メンバー一覧の各行にチェックボックスが表示される
- [ ] 0 件選択状態では「削除」ボタンが disabled
- [ ] 1 件以上選択すると「削除」ボタンが enabled になる
- [ ] ヘッダーチェックボックスをクリックすると全メンバーが一括選択される
- [ ] 全選択時にヘッダーチェックボックスをクリックすると全解除される
- [ ] 一部選択時はヘッダーチェックボックスが indeterminate 表示になる
- [ ] 「削除」ボタンをクリックすると AlertDialog が表示され、選択件数が文言に反映される
- [ ] ダイアログの「削除する」をクリックすると DELETE リクエストが送信される
- [ ] リクエスト body に選択した user_ids が含まれる
- [ ] 削除成功後、メンバー一覧から対象メンバーが消える
- [ ] 削除成功後、member_count が更新される
- [ ] 削除成功後、チェック状態がリセットされ「削除」ボタンが disabled に戻る
- [ ] 複数件を同時に選択して一括削除できる
- [ ] ダイアログの「キャンセル」でダイアログが閉じ、チェック状態が維持される
- [ ] API 500 エラー時はダイアログ内にエラーメッセージが表示され、ダイアログは開いたまま
- [ ] メンバー追加ボタンは削除機能追加後も引き続き表示される（リグレッションなし）
- [ ] 全選択後に削除が成功するとヘッダーチェックボックスが未チェック・disabled にリセットされる
- [ ] ヘッダーチェックボックスの変化に合わせて「削除」ボタンの enabled / disabled が連動する
```

---

## 使用 API

| エンドポイント               | メソッド | 用途                                                                       |
| ---------------------------- | -------- | -------------------------------------------------------------------------- |
| `/api/v1/groups/:id/members` | DELETE   | 選択したメンバーをグループから一括削除する。body: `{ user_ids: number[] }` |
| `/api/v1/groups/:id/members` | GET      | 削除後のメンバー一覧を再取得する                                           |
| `/api/v1/groups/:id`         | GET      | 削除後の member_count を反映したグループ詳細を再取得する                   |

---

## 対応する API 仕様

→ `plans/group/delete-group-members/prd.md`
