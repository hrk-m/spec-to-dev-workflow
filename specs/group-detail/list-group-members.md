# list-group-members — メンバー一覧取得

## 概要

グループに所属するメンバーの一覧を、検索キーワードとページネーションで絞り込みながら取得する。グループ詳細画面の Members セクションに表示される。

---

## 処理フロー（正常系）

```
グループ詳細が表示される / メンバー検索キーワードを入力する / ページボタンを押す
  │
  ├─ useMemberList が groupId・offset・limit・q を元に API リクエストを発行する
  ├─ 初回読み込み時はスケルトンローディングを表示する
  ├─ バックエンドからメンバー一覧と総件数が返る
  ├─ メンバーがイニシャルアバター＋フルネーム形式でリスト表示される
  └─ ページ情報（現在ページ / 総ページ数）が更新される

表示件数ボタン（20 / 50 / 100）を押す
  │
  ├─ クライアントサイドのキャッシュから表示を再計算する（サーバーへの再リクエストなし）
  ├─ ただしキャッシュが不足する場合は追加フェッチが自動で実行される
  └─ ページが 1 にリセットされる
```

---

## 処理フロー（異常系）

```
バックエンドへの通信が失敗する（ネットワークエラー・500 など）
  │
  └─ エラーメッセージがメンバーセクションに表示される

メンバーが 0 件の場合
  │
  └─ 「No members found.」メッセージが表示される
```

---

## 使用コンポーネント・状態

| 要素 | 種別 | 役割 |
|---|---|---|
| `MemberList` | コンポーネント | メンバー検索・表示件数切替・一覧・ページネーションを表示する |
| `MemberRow` | コンポーネント | 個々のメンバー行（アバター＋名前）を表示する |
| `MemberAvatar` | コンポーネント | メンバーのイニシャルアバターを表示する |
| `useMemberList` | カスタム Hook | fetch・検索・表示件数・ページネーション・キャッシュの状態と処理を管理する |
| `cachedMembers` | state | サーバーから取得したメンバーをキャッシュする |
| `total` | state | メンバーの総件数を保持する |
| `currentPage` | state | 現在のページ番号を保持する |
| `perPage` | state | 1 ページあたりの表示件数（20 / 50 / 100）を保持する |
| `searchQuery` | state | メンバー検索キーワードを保持する |
| `totalPages` | derived | 総ページ数を算出する |

---

## 確認観点

```
- [ ] グループ詳細画面を開くとメンバー一覧がスケルトン表示後に表示される
- [ ] メンバーのイニシャルアバターとフルネーム（姓 名）が正しく表示される
- [ ] メンバー検索ボックスにキーワードを入力すると名前で絞り込まれる
- [ ] 検索時にページが 1 にリセットされる
- [ ] 表示件数ボタン（20 / 50 / 100）で表示件数が切り替わる
- [ ] 表示件数変更時にページが 1 にリセットされる
- [ ] Next ボタンで次ページのメンバーが表示される
- [ ] Previous ボタンで前ページのメンバーが表示される
- [ ] 最初のページでは Previous ボタンが無効になる
- [ ] 最後のページでは Next ボタンが無効になる
- [ ] メンバーが 0 件のとき「No members found.」が表示される
- [ ] バックエンドへの通信が失敗するとエラーメッセージが表示される
- [ ] キャッシュ不足時に追加フェッチが自動実行される
```

---

## 使用 API

| エンドポイント | メソッド | 用途 |
|---|---|---|
| `/api/v1/groups/:id/members?limit=N&offset=N&q=keyword` | GET | メンバー一覧を取得する |

---

## 対応する API 仕様

→ `plans/group/list-group-members/prd.md`
