# Spec: {機能名} - {操作名}（例: ユーザー登録）

## 概要

{この操作が何をするかを 1〜2 文で書く。技術用語は使わない}

例: 新しいユーザーを登録する。名前とメールアドレスを入力して送信すると、アカウントが作成される。

---

## 操作フロー（正常系）

{ユーザーの操作を起点に、UI がどう変化しどこにデータが流れるかを示す}

```
ユーザーが {操作} をする
  │
  ├─ {画面/コンポーネント} が {状態変化/イベント発火}
  ├─ {データの送信先・保存先} にデータを渡す
  └─ UI が {結果として何を表示するか} に更新される
```

例:
```
ユーザーが「保存」ボタンをクリックする
  │
  ├─ 設定フォームが入力値を収集する
  ├─ 設定ストアに値を保存する
  └─ 「保存しました」トーストが表示される
```

<details>
<summary>技術詳細（エンジニア向け）</summary>

#### sample-front

```
<!-- AI への指示: フロントエンドの処理フローをここに記述する
  - ユーザー操作 → コンポーネント → API 呼び出し → 画面更新 の流れで書く
  - 使用するコンポーネント名・関数名・状態管理の変数名を具体的に記載する
-->

{コンポーネント名} でユーザーが {操作} をする
  │
  ├─ {バリデーション（例: 入力値チェック）}
  ├─ {API 呼び出し（例: POST /api/resource）}
  ├─ {状態更新（例: store.set / useState）}
  └─ {画面反映（例: 完了メッセージ表示・画面遷移）}
```

#### sample-api

```
<!-- AI への指示: バックエンドの処理フローをここに記述する
  - エンドポイント受信 → バリデーション → ビジネスロジック → レスポンス の流れで書く
  - 使用するハンドラ名・サービス名・DB テーブル名を具体的に記載する
-->

{HTTP メソッド} /api/{resource} を受信する
  │
  ├─ {バリデーション（例: 必須チェック・型チェック）}
  ├─ {ビジネスロジック（例: 重複チェック・DB 操作）}
  └─ {ステータスコード} でレスポンスを返す
```

</details>

---

## 処理の流れ

{操作が行われたときに、裏側でどんな順番で何が起きるかを平易な言葉で書く}

1. {例: 入力された内容に不備がないか確認する}
2. {例: 同じメールアドレスがすでに登録されていないか調べる}
3. {例: 問題がなければアカウントを作成して完了画面を表示する}

---

## API エンドポイント

| 項目 | 内容 |
|---|---|
| メソッド | {GET / POST / PUT / PATCH / DELETE} |
| パス | `/api/{resource}` |
| 認証 | {必要 / 不要} |

---

## エラー時の動作

| 状況 | ユーザーへの表示 |
|---|---|
| {例: 必須項目が未入力} | {例: 「〇〇を入力してください」と表示する} |
| {例: すでに登録済みのメールアドレス} | {例: 「このメールアドレスは使用済みです」と表示する} |

---

## 確認観点(テスト項目)

以下の 2 点を軸に確認項目を列挙する。

- **仕様を満たしているか** — 概要・操作フロー・処理の流れに書いた内容が、実装で正しく動作するか
- **バグが発生しないか** — 想定外の入力・操作・タイミングでも壊れないか

```
- [ ] {例: 正しい内容を入力して送信すると登録が完了する}             # 仕様確認
- [ ] {例: 必須項目が空のまま送信するとエラーが表示される}           # 仕様確認
- [ ] {例: すでに登録済みの情報で送信するとエラーが表示される}       # 仕様確認
- [ ] {例: 同じデータを連続して送信しても二重登録が発生しない}       # バグ確認
- [ ] {例: 通信が途中で途切れても不整合なデータが残らない}           # バグ確認
```
