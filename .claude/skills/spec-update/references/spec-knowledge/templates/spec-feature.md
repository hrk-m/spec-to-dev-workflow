# {機能名} — {機能の概要一行}

## 概要

{この機能で何ができるか。ユーザー視点で 1〜3 文。技術用語は使わない}

---

## 処理フロー（正常系）

```
ユーザーが {操作} をする
  │
  ├─ {画面 / コンポーネント} が {状態変化 / イベント発火}
  ├─ {API 呼び出しのタイミングと目的}
  └─ UI が {結果として何を表示するか} に更新される
```

---

## 処理フロー（異常系）

```
{エラーが発生する状況}
  │
  └─ {ユーザーに表示されるエラーメッセージ・UI の変化}
```

---

## 使用コンポーネント・状態

| 要素 | 種別 | 役割 |
|---|---|---|
| `{ComponentName}` | コンポーネント | {役割} |
| `use{FeatureName}` | カスタム Hook | {管理する状態・処理} |
| `{stateName}` | state | {保持する値} |

---

## 確認観点

```
- [ ] {正常系: 期待どおりに動作することを確認する項目}
- [ ] {異常系: エラーが正しく表示されることを確認する項目}
- [ ] {バグ: 想定外の操作でも壊れないことを確認する項目}
```

---

## 対応する API 仕様

→ `plans/{機能名}/{verb-noun}/prd.md`
