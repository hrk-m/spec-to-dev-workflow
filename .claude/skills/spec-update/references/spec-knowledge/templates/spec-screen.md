# {機能画面} — 画面仕様

## 目的・役割

{この画面で何をするか。ビジネス観点での概要を 1〜3 文で書く。技術用語は使わない}

---

## 識別情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | {表示される画面タイトル} |
| 画面 ID | {screen-id（例: group-list）} |
| URL / パス | {例: /groups} |

---

## 想定利用者

| 項目 | 内容 |
|---|---|
| 対象ユーザー | {例: 一般ユーザー / 管理者 / 両方} |
| 必要な権限・ロール | {例: viewer ロール以上 / admin のみ / 認証不要} |
| アクセス制御 | {例: 未認証はログイン画面へリダイレクト / 権限不足は 403 ページを表示} |

---

## 画面

{`specs/img/` に PNG が存在する場合は img タグで埋め込む。存在しない場合は ASCII アートで代替する。
 リポジトリルートで `bun run screenshot` を実行すると PNG が生成される。
 シナリオは spec ファイルの `## スクリーンショット設定` セクションで定義する。}

<!-- PNG が存在する場合 -->
<img src="./img/{screen-id}.png" width="400" alt="{画面名}" />

<!-- PNG が存在しない場合は ASCII アートで代替 -->
<!--
┌────────────────────────────────────────────────────────────────┐
│  {画面タイトル}                                                 │
│ ─────────────────────────────────────────────────────────────  │
│  {セクションラベル}                                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ {行 1 の内容}                                            │  │
│  │ {行 2 の内容}                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│  [ {ボタン A} ]  [ {ボタン B} ]                                 │
└────────────────────────────────────────────────────────────────┘
-->

<details>
<summary>スクリーンショット設定</summary>

{/spec-screenshot コマンドが自動生成する。手動で編集する場合は以下の形式に従う}

```json
{
  "steps": [
    { "goto": "http://localhost:3000" },
    { "waitForText": "{表示されるはずのテキスト}" },
    { "screenshot": "{screen-id}" }
  ]
}
```
</details>

---

## レイアウトと主要パーツ

| パーツ | 役割 |
|---|---|
| {入力エリア / 一覧 / ボタン など} | {役割の説明} |

---

## 操作手順

> 権限によって操作内容が異なる場合はロールごとに分けて記述する。差異がない場合は「共通」のみ記述する。

**共通（全ロール）**

1. {例: 検索キーワードを入力する}
2. {例: 「検索」ボタンを押す → 一覧が絞り込まれる}

**管理者のみ**

1. {例: 行の「削除」ボタンを押す → 確認ダイアログが表示される}
2. {例: 「OK」を押す → 削除完了・一覧が更新される}

**権限なし（閲覧不可）**

- {例: この画面にアクセスすると 403 ページが表示される / ログイン画面へリダイレクトされる}

---

## ボタン・リンクの機能

| ボタン / リンク | 操作後の動作 |
|---|---|
| {ボタン名 / リンク名} | {クリックで何が起きるか} |

---

## 画面遷移

| 遷移元 | 遷移先 | トリガー |
|---|---|---|
| {元画面名} | {この画面} | {操作・条件} |
| {この画面} | {遷移先画面名} | {操作・条件} |

---

## 入力項目

> 入力項目がない画面はこのセクションを削除する。

| 項目名 | 必須 / 任意 | 形式・制約 |
|---|---|---|
| {項目名} | 必須 | {例: 最大 100 文字、英数字のみ} |
| {項目名} | 任意 | {例: 日付形式 YYYY-MM-DD} |

---

## 前提条件・制約

- {例: ログイン済みであること}
- {例: 管理者権限が必要}
- {例: 前画面で選択した ID が存在すること}

---

## エラー・注意事項

| エラー / 注意 | 内容 |
|---|---|
| {エラー名 or 状況} | {意味・ユーザーへの影響・対応方法} |

---

## 機能一覧

| 機能 | 概要 | 詳細 |
|---|---|---|
| {verb-noun} | {1 行説明} | [{verb-noun}.md](./{verb-noun}.md) |
